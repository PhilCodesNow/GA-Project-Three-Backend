import React from 'react';
import ReactDOM from 'react-dom';

export default (props) => {
    return (
        <div className='header'>
            <h1>Title</h1>
            <img>Logo</img>
        </div>
    );
};